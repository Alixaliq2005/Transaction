server:
  port: 8888
spring:
  datasource:
    url: jdbc:mysql://localhost:1278/Transaction?createDatabaseIfNotExist=true
    username: root
    password: password
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    open-in-view: false
